server:
  port: 7080
  
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true

springdoc:
  enable-native-support: 'true'

logging:
  level:
    org:
      hibernate:
        stat: debug    